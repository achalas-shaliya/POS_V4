import { TransferScreen } from "@/components/transfer/transfer-screen";

export const metadata = { title: "Transfers – POS Console" };

export default function TransfersPage() {
  return <TransferScreen />;
}
